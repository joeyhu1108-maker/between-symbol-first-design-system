# 2026-09-13 完整工程交接验收

在只由 Git 暂存文件导出的干净目录中创建新虚拟环境，通过 requirements.txt 安装依赖。Python 3.9.6，Node.js 24.21.0，使用独立端口18765。

- 作品模块：9项组合测试、6项题字规则测试全部通过，包含66种卡牌组合、重复输入、玩家问题影响和无效输入。
- 基础 Garden：相同种子生成相同像素，不同种子生成不同像素；66对概率逐一核对通过。
- API：幂等任务、单/双卡、参数透传、真实PNG/PDF/JPEG、骰子中转、非法输入、跨域拒绝、私有文件保护通过。不存在的打印队列被拒绝，未下发实体打印。
- 3D：运动连续性、入场、回弹、上盖与纸张检查通过；GLB头与长度通过。
- A4：两张作品均重新排版为210×297 mm，2480×3508像素，四边12 mm白卡，底部无文字；来源路径在新目录可解析。
- 浏览器：主控台canvas与12张卡牌可见，画廊两张最终A4图片加载成功，未捕获JavaScript运行错误。
- 文件：12个ZIP通过CRC校验；无超过100 MiB的单文件；无大小写冲突路径；已导入文件无缺失。
- 凭据：真实设备.env未入库；未发现已知本机密钥被复制进文本或ZIP。虚拟环境、日志、缓存、索引、编辑器本机配置不入库。

机器可读结果见 verification-results.json。最终逐文件哈希见 upload-manifest.json；校验方式为 `python3 scripts/verify_manifest.py`。

这些检查针对当前主程序与最新作品模块。没有执行实体打印、在线AI生成、Cloudflare部署、外部服务器接入、板卡刷机或历史FDM工具；不把这些未验证项目报告为通过。首次浏览器访问早于服务启动曾连接失败，服务就绪后重新加载通过，不属于缺文件问题。
