# Shawn · Mac NFC 与打印测试

[打开测试控制台](https://facilities-have-past-donor.trycloudflare.com/nfc-test.html#control=b9efc4ef6515448cc1b43d309bc5465a4a13b280342437d65a2348c51ac653ca) · [01 号 NFC 标签网址](https://facilities-have-past-donor.trycloudflare.com/nfc-tap.html?card=01)

**控制台链接仅给操作者，NFC 标签只写入「01 号 NFC 标签网址」。** 当前为临时公网测试：源 Mac、后台和隧道都要保持在线；隧道可能短暂断线后恢复。请使用可重写标签，最终固定网址尚未确定。真实打印机出纸尚未验证。

## 先下载两个文件

在 Shawn 的 Mac 打开控制台，点击「下载打印接收程序」和「下载连接配置」，把 `nfc-print-agent.py`、`between-print-config.json` 都放到「下载」文件夹。配置含授权信息，请保管在打印电脑上。

打开「终端」，先检查 Python：

```sh
cd ~/Downloads
python3 --version
```

需要已有可运行的 Python 3。若提示 Xcode 许可或开发者工具，请换用 Mac 上已有的独立 Python 3 路径；此提示不是打印机故障。

## 第一轮：打印机关闭，只测 NFC

1. 打印机保持关闭，先不启动打印接收程序。
2. 在 NFC 写卡工具中选择「网址 / URL」，把上方 **01 号标签网址**写入标签。
3. 控制台选择 `01`，点「生成作品并等待 NFC」。
4. 手机碰标签，点系统弹出的打开提示。确认网页显示触碰已收到，控制台进入等待打印接收端。
5. 同一轮再碰 2–3 次，确认仍是同一轮任务。完成后在控制台「取消尚未领取的本轮任务」，再开始下一轮。

## 第二轮：开启打印机，真实提交

开启打印机，在 Mac「系统设置 → 打印机与扫描仪」添加设备并设为默认打印机。随后在终端运行：

```sh
cd ~/Downloads
python3 nfc-print-agent.py --config between-print-config.json
```

如需指定队列，先查看系统名称，再替换下一行的 `YOUR_CUPS_QUEUE`：

```sh
python3 nfc-print-agent.py --list
python3 nfc-print-agent.py --config between-print-config.json --printer YOUR_CUPS_QUEUE
```

只运行一个接收程序，并保持终端打开。没有默认或指定的可用队列时，它只报告等待，不领取任务。控制台确认接收端在线、队列可用后，重新生成一轮并碰 01 号标签。看到「已提交系统打印队列」后，到打印机旁确认实际出纸，再开始下一轮。关闭打印机电源不一定会让系统队列离线；任务可能已在 Mac 队列中等待设备恢复。

## 网络与重复触碰

最短链路通过后，在主控页点击「进入完整抽卡流程」，从同一个标签页进入以保留联调权限。手机扫码抽一张、AI 抽一张并生成作品，页面左下角会显示本轮需要的 NFC 卡号。完整流程需要给 01–12 号标签分别写入同样的 NFC 网址，只把 `card=01` 改成对应卡号；碰到答案卡后自动推进，并使用这一轮生成的作品打印。

依次使用 **手机 Wi-Fi、手机移动流量、Mac 接手机热点**测试，每种环境新建一轮，并记录：网址能否打开、触碰是否收到、队列是否提交、实际出纸份数。手机与 Mac 不必在同一个网络。

同一轮重复碰标签应只提交一次。测接收端离线时，可先按 Ctrl+C 停止接收程序，再准备一轮并碰标签；看到等待后，恢复同一程序，检查只领取一次。手机本身断网时不能保证触碰已送达，恢复网络后以页面「触碰已收到」为准。

若出现「提交结果不确定」，先检查 Mac 打印队列和设备，不要立刻开启新轮补印，也不要删除 `~/.between-nfc-print-agent.json` 状态文件。`submitted` 仅表示系统接收了任务，不能代替出纸确认。
