# BETWEEN · Symbol First

这套电脑端主控界面以符号作为唯一视觉语言。文字只保留在 `aria-label`、`title` 和状态日志中，保证无文字界面仍可操作、可协作、可验证。

## 符号词典

| 符号 | 含义 | 触发行为 |
| --- | --- | --- |
| `◒` | 输入 / 生长 | 摇成长骰 |
| `⋔` | 关系 / 连接 | 摇关系骰 |
| `✦` | 锁定 / 铸钥 | 生成本次 job 与答案编号，并立即预生成作品 |
| `◇` | 寻找 / 卡墙 | 展开 12 张实体卡 |
| `◉` | NFC 触碰 | 用实体卡编号确认答案 |
| `↯` | 电脑确认 | NFC 未识别时手动确认 |
| `▧` | 打印准备 | 展示已缓存的预生成作品 |
| `⟶` | 发送 | 点击后把缓存图送入小米打印机队列 |
| `✓` | 完成 | 打印流程结束，可取件 |

## 视觉规则

- 主界面保持纯白，黑色与淡紫色只用于动作对比和状态反馈。
- 实体卡使用深靛黑底、双层金属边框、中心发光符号，质感参考收藏卡；卡名不作为视觉主元素。
- 每个阶段只保留一个主动作；Plan B 使用 `↯` 与 NFC `◉` 并列。
- 预生成进度使用细线和百分比，作品图在钥匙阶段就出现，后续打印只复用缓存图。
- 所有动态文案继续写入 DOM 的可访问标签，视觉层隐藏，避免失去键盘、读屏和调试能力。

## 状态顺序

`◒ → ✦ → ◇ → ◉ / ⌂ → ▧ → ⟶ → ✓`

`✦` 触发 `preGenerateArtwork()`。图像缓存到当前 `JOB-*`；即使生成进度仍在推进，`⟶` 也可以立即使用缓存图，避免打印动作被等待阻塞。
